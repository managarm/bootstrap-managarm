default_capabilities: ["builder"]
jobs:
  - name: icu
    tools:
      - host-icu
    packages:
      - icu

  - name: image
    tasks:
      - useful-image-dependencies
      - initialize-empty-image
      - remake-image
      - xz-image
