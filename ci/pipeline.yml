jobs:
  - name: system-toolchain
    tools:
      - system-gcc
    packages:
      - mlibc
      - mlibc-headers

  - name: icu
    tools:
      - host-icu
    packages:
      - icu

  - name: image
    tasks:
      - useful-image-dependencies
      - initialize-empty-image
      - make-image
      - test-image
      - xz-image
