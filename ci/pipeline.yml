jobs:
  - name: system-toolchain
    tools:
      - bootstrap-system-gcc
      - system-gcc
    packages:
      - mlibc
      - mlibc-headers

  - name: icu
    tools:
      - host-icu
    packages:
      - icu

  - name: image
    capabilities: ["fuse"]
    tasks:
      - useful-image-dependencies
      - initialize-empty-image
      - make-image
      - test-image
      - xz-image
